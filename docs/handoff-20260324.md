# 收尾交接记录（2026-03-24）

## 1. 本次目标

- 整理功能与文档
- 清理无用生成文件
- 生成可安装包
- 保留可继续排查的现场信息

## 2. 已完成事项

### 2.1 文档整理
- 新增 docs/feature-and-doc-index.md：统一功能与文档索引
- 新增 docs/handoff-20260324.md：收尾与接力记录

### 2.2 清理动作（低风险）
- 已删除 build（可再生成构建目录）
- 已删除 dist/installer_build（安装器临时构建目录）
- 已删除目录名为 - 的误生成目录（非源码目录）

说明：以上均为可再生成产物或误生成目录，不影响源码与诊断快照。

### 2.3 保留现场
- 保留 diagnostics 下全部 reboot_snapshot_* 快照
- 保留 docs 下验收与需求文档
- 保留运行脚本与配置工具源码

## 3. 当前建议安装包流程

在项目根目录执行：

1. powershell -ExecutionPolicy Bypass -File .\scripts\build_release.ps1
2. powershell -ExecutionPolicy Bypass -File .\scripts\build_gui_installer.ps1

预期输出：
- 上级目录生成 yuninput_setup.exe

## 4. 下次继续优化建议

优先顺序：

1. 候选窗多显示器光标跟随稳定性
2. 排序策略细化（简码级别与 GB2312 优先）
3. 拼音兜底与四码自动上屏边界回归
4. 自造词管理体验打磨

## 5. 快速回归命令

- 安装启用：powershell -ExecutionPolicy Bypass -File .\scripts\install_enable.ps1 -SkipBuild
- 查看运行日志：%LOCALAPPDATA%\yuninput\runtime.log
- 查看安装日志：%LOCALAPPDATA%\yuninput\install.log
- 诊断状态：powershell -ExecutionPolicy Bypass -File .\scripts\inspect_ime_state.ps1
